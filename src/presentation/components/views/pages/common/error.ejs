<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title : 'Lỗi - Hệ Thống Tuyển Dụng' %></title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container my-5">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card shadow-sm">
          <div class="card-body text-center p-5">
            <i class="fas fa-exclamation-triangle text-danger fa-4x mb-4"></i>
            <h2 class="mb-3">Đã xảy ra lỗi</h2>
            <p class="lead mb-4"><%= typeof error === 'string' ? error : 'Đã xảy ra lỗi trên máy chủ. Vui lòng thử lại sau.' %></p>
            
            <% if (typeof error !== 'undefined' && error && typeof error === 'object') { %>
              <div class="alert alert-danger text-left mb-4">
                <p class="mb-1"><strong>Chi tiết lỗi:</strong></p>
                <pre class="mb-0"><%= JSON.stringify(error, null, 2) %></pre>
              </div>
            <% } %>
            
            <a href="/" class="btn btn-primary">Quay về trang chủ</a>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 